<div>
  <p class="discTria" id="metadataShown_$validator.escapeJavascript($group.id)${DOT}${i}"#if ($group.isShowing())
     style="display:block;"#else style="display:none;"#end>
    <a id="hideMetadata_$validator.escapeJavascript($group.name)${DOT}${i}" href="#" title="$group.hideLabel">
      <img id="hideMetadataImage_$validator.escapeJavascript($group.name)${DOT}${i}"
           src="#imageLink("sakai/collapse.gif")" alt="$group.hideLabel" border="0" width="13" height="13" align="top"/>
      $validator.escapeHtml($group.name)
    </a>
  </p>

  <p class="discTria" id="metadataHidden_$validator.escapeJavascript($group.name)${DOT}${i}"#if ($group.isShowing())
     style="display:none;"#else style="display:block;"#end>
    <a id="showMetadata_$validator.escapeJavascript($group.name)${DOT}${i}" href="#" title="$group.showLabel">
      <img id="showMetadataImage_$validator.escapeJavascript($group.name)${DOT}${i}"
           src="#imageLink("sakai/expand.gif")" alt="$group.showLabel" border="0" width="13" height="13" align="top"/>
      $validator.escapeHtml($group.name)
    </a>
  </p>

  <div id="metadataDiv_$validator.escapeJavascript($group.name)${DOT}${i}"#if ($group.isShowing())
       style="display:block;"#else style="display:none;"#end>
    #foreach($tag in $group)
      <p class="shorttext">
        <!-- DO THE RENDERING HERE -->
      </p>
    #end
  </div>
</div>

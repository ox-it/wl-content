<label for="${metadataType.uuid}${DOT}$i">
  $metadataType.name
</label>
<input type="hidden" name="$!{metadataType.uuid}${DOT}${i}" id="${metadataType.uuid}${DOT}${i}"
       value="$!metadataValue"/>
#resources_dateselectionwidget("${metadataType.uuid}" "${i}" $metadataValue $dateFormat)

<label for="${metadataType.uniqueName}${suffix}">
  $metadataType.name
</label>
<input type="hidden" name="$!{metadataType.uniqueName}${suffix} id="${metadataType.uniqueName}${DOT}${i}"
       value="$!metadataValue"/>
#resources_dateselectionwidget("${metadataType.uniqueName}" "${i}" $metadataValue $dateFormat)

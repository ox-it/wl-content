<label for="${metadataType.uuid}${suffix}">$metadataType.name</label>

<select data-placeholder="$metadataType.description" style="width: 350px" name="$!{metadataType.uuid}${suffix}"
        id="$!{metadataType.uuid}${suffix}" #if($multipleMetadataType.contains($metadataType.uuid)) multiple #end class="chzn-select">
  <option></option>
  #foreach($user in ${metadataType.getAllowedValues($site_id)})
    <option value="$user.id" #if($metadataValue.equals($user) || $metadataValue.contains($user)) selected #end>$user.displayName</option>
  #end
</select>
